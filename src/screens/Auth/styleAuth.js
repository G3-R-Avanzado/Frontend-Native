import { StyleSheet } from "react-native"

export const styleAuth = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10
    },
    title: {
        fontSize: 40,
        fontWeight: 'bold'
    },
    input: {
        padding: 10,
        marginVertical: 3,
        backgroundColor: 'white',
        borderRadius: 10
    }
})
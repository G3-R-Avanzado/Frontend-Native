import React from 'react';
import { View,Text } from 'react-native';

const Error = () => {
    return (
        <View>
            <Text>Lo sentimos! Estamos trabajando en resolver todo lo antes posible</Text>
        </View>
    );
};

export default Error;